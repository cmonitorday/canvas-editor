// Constants exposed by the bundled canvas-editor. These mirror the
// values exported from the original bundle but use readable names.

const CanvasEditor = window['canvas-editor'];

export const EDITOR_CLIPBOARD = CanvasEditor.EDITOR_CLIPBOARD;
export const EDITOR_COMPONENT = CanvasEditor.EDITOR_COMPONENT;
export const INTERNAL_CONTEXT_MENU_KEY = CanvasEditor.INTERNAL_CONTEXT_MENU_KEY;
export const LETTER_CLASS = CanvasEditor.LETTER_CLASS;
